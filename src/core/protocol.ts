/** HTTP response contract version, independent of the npm package version. */
export const HTTP_API_PROTOCOL_VERSION = 1

/** SSE event-envelope version, independent of the npm package version. */
export const SSE_PROTOCOL_VERSION = 1
